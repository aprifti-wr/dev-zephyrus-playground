package com.example.exchange;

// We need to create an exchange service that fetches exchange rates.
// The service should be able to fetch rates from the vendor but there is a cost associated with each call.


    // // Vendor interface for fetching exchange rates
    // public interface ExchangeRateVendor {
    //     ? getRate(?...);
    // }
    // cost = $0.01/call

public class ExchangeService {

    public ? getRate(?....) {
      
    }
}
